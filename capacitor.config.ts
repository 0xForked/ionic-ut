import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'exp-ionic-ut',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
